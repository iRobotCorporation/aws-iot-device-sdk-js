# AWS IoT SDK for browser (not official)

This is a modified version of the AWS IoT SDK that only provides the code
necessary for talking with websockets (the rest may be there, but may well
not work).
